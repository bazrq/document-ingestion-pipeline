# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: documentqa
metadata:
  template: documentqa-aspire@0.0.1

services:
  functions:
    project: ./DocumentQA.Functions
    language: dotnet
    host: functionapp

infra:
  provider: bicep
  path: infra
  module: main
